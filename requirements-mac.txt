# Stave Synth — macOS-only Python deps. Installed after requirements.txt by
# install-mac.sh. Linux doesn't need these (C bridge handles audio + MIDI).
sounddevice>=0.4.6      # PortAudio wrapper — replaces the JACK C bridge on Mac.
python-rtmidi>=1.5.0    # Core MIDI access — replaces the a2jmidid bridge.
