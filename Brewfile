# Stave Synth — macOS system dependencies (via `brew bundle`).
# Used by install-mac.sh. Keep this list minimal: Python-level deps live
# in requirements.txt / requirements-mac.txt.

brew "python@3.12"
brew "fluidsynth"
brew "faust"
brew "portaudio"    # sounddevice's C backend (pip wheel usually bundles this,
                    # but an explicit brew install keeps it predictable).
